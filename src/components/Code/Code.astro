---
import CodeBlock from "@/components/Code/Code.jsx";

interface props {
  language: string;
  code: string;
}

const {language, code} = Astro.props;

---

<CodeBlock code={code} language={language} client:visible />
